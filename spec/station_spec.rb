require 'station'

describe Station do 

 it 'has a name' do
  name = name 
  zone = zone
  station = Station.new(name, zone)
  expect(station.name).to eq (name)
 end 

 it 'has a zone' do
  name = name
  zone = zone
  station = Station.new(name, zone)
  expect(station.zone).to eq (zone)
 end 

end 